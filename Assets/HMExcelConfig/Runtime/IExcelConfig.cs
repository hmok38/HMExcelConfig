using System;
using System.Collections.Generic;
using System.Linq;

namespace HMExcelConfig
{
    public interface IExcelConfig
    {
        // public static List<Type> GetAllExcelConfigType()
        // {
        //     var types = AppDomain.CurrentDomain.GetAssemblies().SelectMany(a => a.GetTypes().Where(t => t.GetInterfaces().Contains(typeof(IExcelConfig))))
        //         .ToList();
        //     return types;
        // }
    }
}